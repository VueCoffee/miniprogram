/* components/product/vcontent/vcontent-item/index.wxss */
.vcontent-item {
  display: flex;
  align-items: center;
  margin-top: 20px;
}
.vcontent-item-image {
  width: 80px;
  height: 80px;
}
.vcontent-item-content {
  flex: 1;
  margin-left: 10px;
}
.vcontent-item-title {
  font-size: var(--zh-sixteen-size);
  color: var(--text-color);
  font-weight: bold;
  margin-left: 5px;
}

.vcontent-item-title-en {
  font-size: var(--zh-ten-size);
  color: var(--gary-color);
}
.vcontent-item-bottom {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-top: 3px;
}
.vcontent-item-prices {
  display: flex;
  align-items: baseline;
}

.vcontent-item-prices text:nth-of-type(1) {
  font-size: var(--zh-ten-size);
  color: var(--text-gary-color);
}
.vcontent-item-prices text:nth-of-type(2) {
  font-size: var(--zh-fourteen-size);
  color: var(--text-color);
  font-weight: bold;
}

.vcontent-item-num-view {
  display: flex;
  align-items: center;
}
.vcontent-item-num-view text {
  margin-bottom: 3px;
  text-align: center;
  width: 40px;
  font-size: var(--zh-fourteen-size);
  color: var(--text-color);
}
