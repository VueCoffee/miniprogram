/* components/tag/tag_gold/index.wxss */
.tag-gold {
  background-color: var(--gold-color);
  color: var(--white-color);
  font-size: var(--zh-twelve-size);
  padding: 1px 5px;
  border-radius: 20px;
}